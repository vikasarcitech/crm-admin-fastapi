# Fully resolved dependency set, transitives pinned. Generated with
# `pip freeze` from the environment this project was tested in.
#
# Use this for Docker builds and CI so a transitive release can't change
# behaviour between builds:
#     pip install --no-deps -r requirements.lock.txt
#
# Use requirements.txt for local development, where patch-level drift in
# transitives is fine and upgrades are easier.
annotated-types==0.8.0
anyio==4.15.0
asyncpg==0.30.0
bcrypt==4.2.1
certifi==2026.7.22
click==8.5.0
dnspython==2.8.0
email_validator==2.2.0
fastapi==0.115.6
h11==0.16.0
httpcore==1.0.9
httptools==0.8.0
httpx==0.28.1
idna==3.19
pydantic==2.10.4
pydantic_core==2.27.2
python-dotenv==1.0.1
PyYAML==6.0.3
starlette==0.41.3
typing_extensions==4.16.0
uvicorn==0.34.0
uvloop==0.22.1
watchfiles==1.2.0
websockets==17.1
