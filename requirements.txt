fastapi==0.115.6
uvicorn[standard]==0.34.0
asyncpg==0.30.0
bcrypt==4.2.1
python-dotenv==1.0.1
httpx==0.28.1
pydantic>=2.13.0
email-validator==2.2.0

# Multipart form parsing, for media uploads (app/routers/media.py).
python-multipart==0.0.32

# HTML sanitization for rich-text content (app/sanitize.py). Rust
# bindings to `ammonia` — an allow-list cleaner, not a regex pass.
nh3==0.3.7

# Image validation and responsive WebP/AVIF derivatives (app/imaging.py).
Pillow==12.3.0

# AWS: S3 media storage, CloudFront invalidation, backup upload. Imported
# lazily, so an install using MEDIA_STORAGE=local never loads it.
boto3==1.43.89
